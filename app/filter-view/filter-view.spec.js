'use strict';

describe('filter-view module', function() {

    beforeEach(module('filterView'));

    describe('filterView controller', function(){

        it('should ....', inject(function($controller) {
            //spec body
            var filterViewCtrl = $controller('filterViewCtrl');
            expect(filterViewCtrl).toBeDefined();
        }));

    });
});